services:
  app:
    image: game-api
    env_file:
      - .env
    ports:
      - "${API_HOST_PORT:-8020}:8000"
    build:
      context: ./backend
      dockerfile: Dockerfile
    command:
      - /src/entrypoints/gunicorn.sh

  frontend:
    image: game-frontend
    env_file:
      - .env
    ports:
      - "${FRONTEND_HOST_PORT:-3020}:3000"
    build:
      context: ./frontend
      dockerfile: Dockerfile
      args:
        NEXT_PUBLIC_API_BASE_URL: ${NEXT_PUBLIC_API_BASE_URL:-http://localhost:8010}
        NEXT_PUBLIC_GOOGLE_SHEETS_SERVICE_ACCOUNT_EMAIL: ${NEXT_PUBLIC_GOOGLE_SHEETS_SERVICE_ACCOUNT_EMAIL:-}
        NEXT_PUBLIC_GOOGLE_CLIENT_ID: ${NEXT_PUBLIC_GOOGLE_CLIENT_ID:-}
    depends_on:
      - app
